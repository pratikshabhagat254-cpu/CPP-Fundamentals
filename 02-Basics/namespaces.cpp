#include <iostream>   // Header file for input and output

int main() {

    // std::cout is used to display output on the screen
    // 'std::' means we are using cout from the standard namespace
    std::cout << "Using std namespace explicitly";

    // return 0 means the program ended successfully
    return 0;
}